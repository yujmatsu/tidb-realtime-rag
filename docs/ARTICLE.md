---
title: "日本語RAGの「鮮度」を測る ── TiDBで踏んだ実装の落とし穴と測定方法"
emoji: "🥬"
type: "tech"
topics: ["tidb", "rag", "vectorsearch", "ai", "database"]
published: false
---

> Zenn コンテスト「TiDBで作るAI時代のデータ基盤」(PingCAP) 応募作。
> **手を動かして得た一次情報**と**測り方**をまとめた実装ノートです。誇張を避け、
> **TiDBが有利でなかった部分も正直に**載せます。全コード・データ・再現手順は公開（末尾）。

## この記事で得られるもの（3つの持ち帰り）

業務データに答えるRAGを TiDB で実装する中で、**ドキュメントには載っていない一次情報**と、
RAGの鮮度を**定量評価する方法論**が貯まりました。本記事の中身はこの3つです。

1. **日本語全文検索の落とし穴と設計レシピ**（`MULTILINGUAL` パーサは形態素解析ではない）
2. **TiDBの「鮮度境界」の実測**（点参照は即時 / TiFlash 列ストアは ≈142ms の同期遅延）
3. **RAGの鮮度を"測る"方法論**（「鮮度誤答率」を質問分布 × 同期間隔の関数として計測）

### この記事の前提（30秒で）

本文で使う用語を、初学者向けに最小限だけ先に。

- **RAG**：LLMが回答する前に、DBから関連情報を検索して渡す仕組み（Retrieval-Augmented Generation）。
- **ベクトル検索 / 埋め込み**：テキストを数値の並び（＝埋め込み）に変換し、「意味の近さ」で検索する手法。キーワード一致ではなく意味で引ける。
- **全文検索**：単語のキーワード一致で引く検索（本記事§3で日本語の落とし穴を扱う）。
- **TiDB / TiDB Cloud Zero**：MySQL互換のサーバーレス分散DB。**1つのDBで SQL＋ベクトル検索＋全文検索＋トランザクション(ACID)** が書ける。**TiDB Cloud Zero** は `curl` 1発・サインアップ不要で立ち上がる無料の使い捨てクラスタ（本記事の全検証はこれで実施）。
- **HTAP（行ストア TiKV / 列ストア TiFlash）**：書き込み・点参照に強い行ストア(TiKV)と、集計・分析に強い列ストア(TiFlash)を**1つのDBに同居**させる仕組み。TiDBの特徴。
- **本記事が比較する2つの構成**：
  - **同期型RAG**：ベクトルDBとRDBを**別々に**持ち、定期/CDCで同期する一般的な構成 → 同期の遅れでデータが古くなる。
  - **ライブ参照RAG**：データと埋め込みを**同じDB**に置き、常に最新を参照する構成。

> 先に正直に言うと、本記事の結論のひとつ「データと埋め込みを**単一エンジン**に置けば鮮度は保てる」は、
> **当たり前で、TiDB固有でもありません**（Postgres+pgvector でも同じ）。
> なので本記事の価値は"TiDB圧勝"ではなく、**実装の一次情報と鮮度の測り方**にあります。

---

## 1. なぜ「鮮度」が問題なのか（30秒で）

ある商品が**完売した直後**に、同じ質問を「定期同期型RAG」と「ライブ参照RAG」に投げた実ログです。

```
👤 商品5は在庫ありますか？
❌ 同期型（VectorDB+RDBを定期同期）: 「はい、12個在庫があります」 ← 古い。もう売り切れ
✅ ライブ参照:                      「在庫切れ（0個）です」      ← 正しい
```

LLMもプロンプトも悪くありません。**検索基盤が古いデータを返している**だけ（stale RAG）。
そして実害は無視できません。「**更新直後の商品を必ず聞く**」＝鮮度が最も問われる**上限ケース**で計測したところ：

![同期型RAGの誤答内訳（鮮度感度の上限ケース）](/images/realtime-rag-tidb/fig1b_harm.png)
*図1: 鮮度感度の上限ケースで100問中43問が古い答え。うち30問は「売り切れを“在庫あり”と偽答」（＝在庫0なのにスナップショットが>0）。*

- 100問中 **43問が誤答**、うち **30問**は「実在庫0なのに“在庫あり”と答える」危険な向き（判定基準：スナップショットの在庫>0 ∧ 実在庫=0）。
- ※ この43%は「変化した項目を狙って聞く」**上限ケース**。一様にカタログを聞くと0〜3%（後述）。実運用のクエリ分布はこの**間**に乗ります。

> 「鮮度は基盤の問題」だと分かったら、次に必要なのは**それをどう測るか**です。

---

## 2. RAGの鮮度を“測る”：鮮度誤答率という方法論 ★

漠然と「同期遅延は悪」と言う代わりに、**測定可能な指標**に落とします。

### 定義（原因をLLMから切り離す）
> **鮮度誤答率** = 同一質問を同時刻に「同期型」「ライブ」へ投げ、**ライブが正答 ∧ 同期型が誤答**の割合。
> 検索ロジック・埋め込み・乱数シード・`temperature=0` を**両者で完全固定**し、差分を同期遅延に帰属させる。

### 実験設定（何を・どう測ったか）
- **データ**：商品80件のカタログ（在庫・説明・カテゴリを持つ）。決定的に生成（シード固定で再現可能）。
- **更新**：在庫数をランダムに変える操作を100件、決定的な順序で適用（在庫切れ＝0を多めに含む）。
- **質問**：100件。「この商品の在庫はある？」を、次の2通りの**当て方**で自動生成し、各時点の真の在庫と照合。
  - **uniform（下限）**：全商品から一様にランダムに聞く（在庫変化とは無相関）。
  - **active（上限ケース）**：**更新直後の商品を必ず聞く**構成。「在庫が動いた直後に確認する」行動が100%集中した**理論的な上限**で、実利用そのものではなく"鮮度が最も問われる最悪寄り"のケース。
- **同期間隔**：同期型を CDC相当(@1)/@5/@20/@60更新ごと、と変えて比較。

![鮮度誤答率 vs 同期間隔（active/uniform）](/images/realtime-rag-tidb/fig2_freshness_error.png)
*図2: 同期間隔が広いほど誤答増。上限ケース(active)で顕著。ライブ参照は常に0%。*

| 条件 | uniform（無相関＝下限） | **active（上限ケース）** |
|---|---|---|
| ライブ参照 | **0.0%** | **0.0%** |
| 同期@1（CDC理想） | 0.0% | 0.0% |
| 同期@5 | 0.0% | **35.0%** |
| 同期@20 | 1.0% | **43.0%** |
| 同期@60 | 3.0% | **43.0%** |

> **実際のユーザーのクエリ分布は uniform(下限) と active(上限) の間**に乗ります。自サービスのクエリログをこの軸で分類すれば、自分の鮮度リスクの露出度が見積もれます。

### この測定から導ける洞察
1. **鮮度誤答率 ＝「同期間隔 × 質問が更新と相関するか」の関数**。実分布は uniform(下限) と active(上限) の間。
   - 補足：active で @5=35% → @20以降=43% と頭打ちになるのは、更新100件が質問期間内にほぼ出尽くし、**同期間隔が「質問対象が変化する間隔」を超えると誤答が飽和する**ため。つまり43%はこの設定での天井。
2. **CDC(@1) ≈ ライブ**：毎更新同期すれば鮮度差はほぼ消える。ただしCDCは同期パイプラインの運用・コストを伴う。
3. **持ち帰り（方法論）**：自分のRAGでも「同一質問を“ライブ版”と“同期版”に同時投入し、ライブ正答∧同期誤答率」を測れば、鮮度リスクをLLM品質と切り離して定量化できる。クエリログを active/uniform に分類すれば、自サービスの露出度も見える。

---

## 3. 一次情報①：日本語全文検索の限界と設計レシピ ★

ここからが「動かして初めて分かる」一次情報です。

TiDB の `FULLTEXT ... WITH PARSER MULTILINGUAL` は、**日本語の形態素解析ではありません**。実測した不安定挙動：

| 検索語 | 期待 | 実際 |
|---|---|---|
| 防災 | 「渋谷区の防災…」に一致 | **不一致（漏れ）** |
| 渋谷 | 「渋谷区…」に一致 | **不一致（漏れ）** |
| 渋谷区 | 渋谷区のみ | **新宿区も誤ヒット** |
| 子育て / お知らせ | 該当 | ✅ 一致 |

漏れと誤ヒットが同時に起きます。一方、**英数字トークン（SKU・型番・エラーコード）は正確にヒット**します。

なぜか（観察からの推測）：日本語は英語のように単語を空白で区切らない（分かち書きがない）ため、`MULTILINGUAL` パーサが**語の境界を正しく取れていない**と見られます。「渋谷区」が「新宿区」に誤ヒットするのは、**文字単位や部分的なトークン化**で「区」のような共通文字に引っ張られている可能性があります。逆に英数字（SKU・型番）は空白やハイフンで自然に区切られるため、安定してヒットします。※内部仕様は公式ドキュメントで最新を確認してください（ここでは実測挙動と、それに基づく設計判断を示します）。

### 設計レシピ（そのまま持ち帰れる）
- **日本語の自然文 → ベクトル検索**に寄せる（意味で引く）。
- **全文検索は英数字（SKU・型番・カテゴリ記号・エラーコード）に限定**する。
- 全文一致は `fts_match_word('語', カラム)`（**小文字・WHERE内で単独使用**。他関数と組み合わせ不可）。
- ベクトル列・全文インデックスは **TiFlash レプリカが前提**。`AUTO_RANDOM` 主キーとの併用は可。

---

## 4. 一次情報②：TiDBの「鮮度境界」を実測する ★

「単一エンジンならいつでも最新」と言っても、**どこまで即時で、どこから遅延するか**が実装では効きます。
前提として、TiDB は同じテーブルを**行ストア(TiKV)＝書き込み・点参照用**と**列ストア(TiFlash)＝分析・大規模ベクトル用**の両方で保持します（HTAP）。鮮度の振る舞いはこの2つで異なります。

![TiDB内部の鮮度境界（実測）](/images/realtime-rag-tidb/fig5_freshness_boundary.png)
*図3: 更新後、点参照(TiKV行ストア)は即時。分析/大規模ベクトル(TiFlash列ストア)は ≈142ms の同期。*

- **点参照（TiKV 行ストア）＝即時**。在庫を0に更新した瞬間、検索に反映（read-your-writes＝書き込み直後に読んでも必ず最新値が返る保証。5/5ケースで確認）。
  update+read の往復は中央値 ≈331ms だが、これは us-west-2 への**ネットワークRTTが大半でエンジン遅延ではない**。
- **TiFlash 列ストアの同期遅延 ≈ 142ms**。

> 測定方法（再現用）：ある行を更新→`/*+ read_from_storage(tiflash[...]) */` で列ストアを強制し、新しい値が返るまで 100ms 間隔でポーリング→反映までの時間を記録。**n=3 の実測**で ~142ms（粒度100msのため実遅延は数十ms〜）。**1環境・少数回の値**なので、絶対値は環境依存と捉え、設計には「点参照=即時／分析系=~0.1s級の遅延がある」という**桁感**を使ってください。

### 設計指針
- 「在庫ある？」のような**鮮度がシビアな点照会は行ストア（TiKV）に寄せる**＝即時。
- **大規模ベクトル検索・分析集計は列ストア（TiFlash）経由で ~0.1s 級の同期**を許容する前提で設計する。

### 実用パターン：最新状態 × 意味検索を1クエリで
単一エンジンなら、現在の在庫（最新OLTP）でフィルタしつつ意味検索を1クエリで書けます（直前に在庫0化した商品は即除外＝read-your-writes）。

```sql
SELECT product_id, name, stock, VEC_COSINE_DISTANCE(emb, ?) AS dist
FROM products
WHERE stock > 0                  -- 今この瞬間の在庫
ORDER BY dist ASC LIMIT 5;
```

---

## 5. 設計判断：単一エンジンにするか（正直な「なぜTiDB」）

ここは誇張せず切り分けます。

- 図2で見た**鮮度の勝ちは「単一エンジン化」の勝ち**であって、**TiDB固有ではありません**。元データと同じDBにベクトルを置けば、**Postgres+pgvector でも同じ図が描けます**。
- では TiDB を選ぶ"正直に言える"理由は：
  1. **`curl` 1発の使い捨てクラスタ**（TiDB Cloud Zero）でサインアップ不要、ベクトル＋全文＋SQL＋ACID＋read-your-writes が1接続で揃う（同期ジョブも別OLAPも不要）。
  2. **1クエリで「最新状態 × 意味検索 × 全文」**が書ける（貼り合わせ不要）。
  3. **将来スケール時に同じSQLのまま HTAP/水平スケールへ拡張できる**（再アーキ不要）。

### HTAP優位を測ったが、本検証規模では出なかった（正直に）
TiDB の目玉は HTAP（行ストア＋列ストア）。「列ストアなら大規模集計が速いはず」と期待し、50k〜500k行で集計レイテンシを測りました。

![集計レイテンシ TiKV(行) vs TiFlash(列)（50k-500k）](/images/realtime-rag-tidb/fig4_htap_latency.png)
*図4: GROUP BY 集計。この規模・環境では列ストアの明確な優位は出ず（0.7〜1.3x＝ノイズ域）。*

| 規模 | TiKV(行) | TiFlash(列) | 速度比 |
|---|---|---|---|
| 50,000 | 205ms | 205ms | 1.0x |
| 100,000 | 197ms | 301ms | 0.7x |
| 200,000 | 263ms | 259ms | 1.0x |
| 500,000 | 412ms | 308ms | 1.3x |

- **本検証の規模・環境では HTAP の明確な優位は出ませんでした**。原因：集計が軽く（GROUP BY 5カテゴリ）、レイテンシは**ネットワークRTT支配**（200-400ms）、無料の使い捨てクラスタの小規模 TiFlash。
- よって本記事は **「TiDBはHTAPで速い」とは主張しません**。HTAP の真価は数百万行・重い分析の領域で、本検証の範囲外（将来の余地）。**都合の悪い結果も隠さず載せます。**

![貼り合わせ3システム vs TiDB単一エンジンの構成比較](/images/realtime-rag-tidb/diagram-02-architecture.png)
*図5: 「貼り合わせ」構成（左, 同期遅延あり）と単一エンジン（右, 鮮度ゼロ遅延）。*

---

## 6. 運用知見（ハマりどころ）

- **使い捨てクラスタ**：`curl -s -X POST https://zero.tidbapi.com/v1beta1/instances -d '{"tag":"demo"}'` で host/user/pass が即返る（MySQL互換・ベクトル/全文対応）。
- **接続耐性**：長時間運用ではDB接続のアイドル切断が起きる → **ping再接続＋例外時リトライ**を実装すべき（本検証でも実際に切断に遭遇）。
- **TiFlash レプリカ前提**：ベクトル/全文インデックスは列ストアレプリカが要る。INSERT直後は列ストア反映に遅延。

---

## 7. 正直な境界（誇張しないために）

- 「同期型」は定期/CDC同期構成の**挙動を再現した実装**（実Pineconeではない）。
- 鮮度の勝ちは「単一エンジン化」によるもの（§5）。本記事は TiDB の性能優位を主張しない。
- **HTAP の性能優位は本検証の規模では出なかった**（§5）。主張しない。
- デモは小規模・軽量埋め込み（64次元・商品80）。誤答率は「傾向」「上限ケース」として提示。

---

## 8. 結論

- **RAGの鮮度は LLM ではなく“基盤の鮮度”の問題**であり、**測れる**（鮮度誤答率：上限ケースで43%、その多くは“売り切れを在庫ありと偽る”危険な向き。実分布は0〜43%の間）。
- **単一エンジン化で鮮度誤答は0**にできる（§5の通り、これは単一エンジン化の効果）。
- TiDB の実利は **サーバーレスの手軽さ＋1クエリ統合＋拡張余地**。HTAP の性能優位は本検証規模では確認できず、誇張しない。
- 実装で本当に効くのは **日本語全文の設計レシピ**と**鮮度境界（点参照=即時 / 列ストア≈142ms）**という一次情報。

**チェックリスト（“単一エンジンRAG”が向くか）**

| 質問 | Yes が多いほど向く |
|---|---|
| ユーザーは「今の状態（在庫・ステータス）」を聞くか？ | □ |
| データは頻繁に更新されるか？ | □ |
| VectorDB+RDB+同期 の運用が重いと感じるか？ | □ |
| サーバーレスで小さく始めて拡張したいか？ | □ |

### この記事を実務に使うなら（次の一歩）
1. **まず自分のRAGで鮮度誤答率を測る**：同一質問を「ライブ版」「同期版」に同時投入し、`ライブ正答 ∧ 同期誤答`の割合を出す（§2の方法をそのまま流用）。
2. **全文検索に日本語の自然文を入れているなら、設計を見直す**：日本語はベクトル、英数字(SKU/型番)は全文、に役割分担（§3）。
3. **鮮度がシビアな点照会は行ストア（即時反映）に寄せる**（§4）。

発展：本番規模でのHTAP検証、マルチエージェント、**mem9（エージェント向け長期記憶ライブラリ）**によるユーザー文脈のパーソナル化。

---

## 再現リポジトリ
- コード・スキーマ・計測スクリプト・生成グラフ一式：（リポジトリURL）
- `scripts/rt_freshness.py`（鮮度誤答率）/ `rt_primary.py`（鮮度境界）/ `rt_demo.py`（NL会話）/ `rt_charts.py`（図）

---

## 図解生成プロンプト（Nano Banana用）

データ図（図1/2/3/4）は実測から matplotlib 生成済み。概念図2点（図5構成図・冒頭タイムライン）を以下で生成し `images/realtime-rag-tidb/` に配置してください。

---

### diagram-01-timeline.png（冒頭：同期遅延窓のタイムライン）

**ファイル名**: `diagram-01-timeline.png`
**目的**: 在庫更新の瞬間から次の同期までの「誤答窓」で、同期型だけが古い嘘をつき、ライブ参照は即正答することを時系列で示す。
**構成**:
- 横長レイアウト。中央に左→右の水平な時間軸。
- マーカー3つ:「更新 在庫12→0」「ユーザー質問」「同期実行」。
- 「更新」〜「同期実行」を薄い赤帯で塗り「同期遅延窓」とラベル。
- 上段=同期型・下段=ライブの2レーン。質問位置に回答吹き出し。同期実行マーカー以降は同期型も緑に変わる。
**ラベル**: 「更新 12→0」「質問」「同期実行」「同期遅延窓」「同期型」「ライブ」「在庫あります 12個」「在庫切れ 0個」
**要素**: 水平実線矢印（時間軸）/縦線＋丸（マーカー）/薄赤帯(#FBEAEA・境界#E74C3C)/角丸吹き出し（同期型=赤枠✕・ライブ=緑枠✓）
**吹き出し**: 「ポイント: この窓の間だけ古い嘘が出る」（右上）
**共通スタイル**: 白背景、余白多め、Noto Sans JP、色は赤（#E74C3C）・緑（#27AE60）・グレー（#888888）の3色、影なし

---

### diagram-02-architecture.png（図5：貼り合わせ vs 単一エンジン）

**ファイル名**: `diagram-02-architecture.png`
**目的**: 「VectorDB＋RDB＋同期」の貼り合わせ（鮮度遅延あり）と TiDB単一エンジン（鮮度ゼロ遅延）を横並び比較し、3箱 vs 1箱を一目で示す。
**構成**:
- 横並び2パターン。左「貼り合わせ」=縦に「RDB 在庫」「ベクトルDB」「分析DB」、RDB→ベクトルDBへ破線矢印（時計アイコン）。右「TiDB単一エンジン」=1つの大箱に「SQL/ベクトル/全文/ACID」を内包。右を緑ハイライト。
**ラベル**: 「貼り合わせ」「単一エンジン」「RDB 在庫」「ベクトルDB」「分析DB」「定期/CDC同期」「SQL」「ベクトル」「全文」「ACID」「同期遅延で誤答」「鮮度ゼロ遅延」
**要素**: 角丸矩形/破線矢印＋時計アイコン(グレー#888888)/右は紺枠(#003366)大箱＋薄緑背景(#EAF7EF)・緑枠(#27AE60)
**吹き出し**: 「結論: 単一エンジン化で同期遅延が消える」（右上）
**共通スタイル**: 白背景、余白多め、Noto Sans JP、色は紺（#003366）・緑（#27AE60）・グレー（#888888）の3色、影なし

---

> 図解はこの2枚＋実測データ図4枚で十分です。これ以上は図解過多になるため非推奨。
